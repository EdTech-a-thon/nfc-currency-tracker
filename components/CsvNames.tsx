"use client";
export function CsvNames() { return <label className="label">Or choose a CSV file<input className="field" type="file" accept=".csv,text/csv" onChange={async (event) => { const file = event.target.files?.[0]; const textarea = event.currentTarget.form?.elements.namedItem("names") as HTMLTextAreaElement | null; if (file && textarea) { const lines = (await file.text()).split(/\r?\n/).filter(Boolean); const first = lines[0]?.toLowerCase(); textarea.value = (first === "name" || first === "displayname" ? lines.slice(1) : lines).map((line) => line.split(",")[0].replace(/^"|"$/g, "")).join("\n"); } }} /></label>; }
