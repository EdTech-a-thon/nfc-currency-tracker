import { currentTeacher } from "@/lib/auth";
import { resolveCard } from "@/lib/cards";
import { db } from "@/lib/db";
import { CardAward } from "@/components/CardAward";
import Link from "next/link";
export const dynamic = "force-dynamic";
export default async function CardView({ params }: { params: Promise<{ token: string }> }) { const { token } = await params; const result = await resolveCard(token); if (!result) return <main className="grid min-h-screen place-items-center p-5"><div className="panel max-w-md p-8 text-center"><p className="font-bold text-[#e85d43]">NFC Currency Tracker</p><h1 className="mt-3 text-3xl">This card isn’t assigned yet</h1><p className="mt-3 text-slate-600">Ask your teacher to assign or replace this card.</p></div></main>; const teacher = await currentTeacher(); const [transactions, balanceResult] = await Promise.all([db.transaction.findMany({ where: { studentId: result.student.id }, orderBy: { createdAt: "desc" }, take: 30 }), db.transaction.aggregate({ where: { studentId: result.student.id }, _sum: { amount: true } })]); const store = result.student.classroomId ? await db.storeItem.findMany({ where: { classroomId: result.student.classroomId, active: true }, orderBy: { sortOrder: "asc" } }) : []; const total = balanceResult._sum.amount ?? 0; const classroom = result.student.classroom!; const ownTeacher = teacher?.id === result.student.teacherId; const presets = ownTeacher ? await db.awardPreset.findMany({ where: { classroomId: classroom.id }, orderBy: { sortOrder: "asc" } }) : []; return <main className="mx-auto grid min-h-screen max-w-xl gap-5 p-4 py-8"><section className="overflow-hidden rounded-[2rem] bg-[#23312c] p-7 text-white"><p className="font-bold text-[#f9bd72]">{classroom.name}</p><h1 className="mt-2 text-3xl">{result.student.displayName}</h1><p className="display mt-8 text-7xl text-[#f9bd72]">{classroom.currencySymbol}{total}</p><p>{classroom.currencyName}</p></section>{ownTeacher && <CardAward studentId={result.student.id} presets={presets} currencyName={classroom.currencyName} />}{ownTeacher && <Link className="btn" href={`/app/student/${result.student.id}`}>Open full teacher controls</Link>}<section className="panel overflow-hidden"><h2 className="p-5 text-xl">Recent activity</h2>{transactions.map((entry) => <div className="flex justify-between border-t border-black/10 p-4" key={entry.id}><div><strong>{entry.reason}</strong><p className="text-sm text-slate-500">{entry.createdAt.toLocaleDateString()}</p></div><span className="display text-xl">{entry.amount > 0 ? "+" : ""}{entry.amount}</span></div>)}{!transactions.length && <p className="p-5">No activity yet.</p>}</section><section className="panel p-5"><h2 className="text-xl">Class store</h2><div className="mt-3 grid gap-2">{store.map((item) => <div className="flex justify-between rounded-xl bg-black/5 p-3" key={item.id}><span>{item.name}</span><span className={total >= item.price ? "font-bold text-green-700" : "text-slate-500"}>{classroom.currencySymbol}{item.price} · {total >= item.price ? "Affordable" : `${item.price - total} to go`}</span></div>)}{!store.length && <p>The store menu is empty right now.</p>}</div></section></main>; }
